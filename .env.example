# 환경 설정 파일
NODE_ENV=production
PORT=5000

# 보안 설정 (교육용 - 실제로는 보안상 취약함)
DISABLE_SECURITY=true
ALLOW_CORS=true
DISABLE_HELMET=true

# 데이터베이스 설정 (JSON 파일 사용)
DB_TYPE=json
DATA_PATH=./data

# 로깅 설정
LOG_LEVEL=debug
ENABLE_ACCESS_LOG=true

# 업로드 설정
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=10485760
